'use client';
import React from 'react';
// import LexicalEditor from './lexical-editor';

const Sample = () => {
  return <div>{/* <LexicalEditor id="sample" data={data} onChange={setState} /> */}</div>;
};

export default Sample;
